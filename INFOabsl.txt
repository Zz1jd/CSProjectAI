INFO:absl:Best score of island 0 increased to -1161.9876066089935
INFO:absl:Best score of island 1 increased to -1161.9876066089935
INFO:absl:Best score of island 2 increased to -1161.9876066089935
INFO:absl:Best score of island 3 increased to -1161.9876066089935
INFO:absl:Best score of island 4 increased to -1161.9876066089935
INFO:absl:Best score of island 5 increased to -1161.9876066089935
INFO:absl:Best score of island 6 increased to -1161.9876066089935
INFO:absl:Best score of island 7 increased to -1161.9876066089935
INFO:absl:Best score of island 8 increased to -1161.9876066089935
INFO:absl:Best score of island 9 increased to -1161.9876066089935
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1161.9876066089935
Sample time  : None
Evaluate time: 3.1532821655273438
Sample orders: None
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    """
    Enhanced version of node selection scores with additional constraints.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand >= 10:
                scores[idx] -= adjustment * 1.5  # Further favor high-demand nodes
            elif demand < 5:
                scores[idx] -= adjustment * 0.5  # Slightly favor low-demand nodes
            else:
                scores[idx] -= adjustment
        else:
            if demand > remaining_capacity:
                scores[idx] += adjustment * 2  # Heavily penalize nodes exceeding capacity
            else:
                scores[idx] += adjustment * 0.5  # Penalize moderately for near capacity nodes
            
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1177.5911945339337
Sample time  : 3.48029488325119
Evaluate time: 0.8668558597564697
Sample orders: 2
======================================================


INFO:absl:Best score of island 6 increased to -1123.0234274825646
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    return -scores
------------------------------------------------------
Score        : -1123.0234274825646
Sample time  : 3.48029488325119
Evaluate time: 1.6467444896697998
Sample orders: 3
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            # Penalize nodes exceeding capacity
        
        for i in range(len(node_demands)):
            if i != current_node:
                demands_sum = np.sum(node_demands)
                if demands_sum > remaining_capacity:
                    scores[i] -= demands_sum / remaining_capacity  # Adjust scores based on total demand
        
        if current_node % 2 == 0:
            scores = np.where(scores > np.mean(scores), scores * 0.9, scores)  # Reduce scores for even node index
        
        scores = np.where(scores < 0, scores / 2, np.minimum(scores, distance_data[current_node] * 1.2))  # Adjust scores based on distance
        
    return -scores
------------------------------------------------------
Score        : None
Sample time  : 3.48029488325119
Evaluate time: 3.5429091453552246
Sample orders: 4
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment  # Penalize nodes exceeding capacity
    
    min_score = min(scores)
    max_score = max(scores)
    
    for i in range(len(scores)):
        if scores[i] == min_score:
            scores[i] += distance_data[current_node][i]  # Add distance to encourage other alternatives
        elif scores[i] == max_score:
            scores[i] -= distance_data[current_node][i]  # Reduce distance for higher scoring nodes
    
    return -scores
------------------------------------------------------
Score        : -3788.3845878596626
Sample time  : 3.48029488325119
Evaluate time: 1.1991653442382812
Sample orders: 5
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    # Initialize variables for tracking the best node and its score
    best_node = -1
    best_score = float('inf')
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
        
        # Update the best node if the current node has a better score
        if scores[idx] < best_score:
            best_node = idx
            best_score = scores[idx]
    
    if best_node != -1:
        scores[best_node] -= math.sqrt(remaining_capacity)  # Prioritize best node even further
    
    return -scores
------------------------------------------------------
Score        : -1161.9876066089935
Sample time  : 2.310514450073242
Evaluate time: 0.8910186290740967
Sample orders: 6
======================================================


INFO:absl:Best score of island 0 increased to -1160.9910409235176
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            if remaining_capacity > 0.5 * np.sum(node_demands):  
                scores[idx] -= adjustment / 2  # Consider half capacity for high-demand nodes
            else:
                scores[idx] += adjustment  # Penalize nodes exceeding capacity
    
    # Additional adjustments based on distance to depot
    for idx, dist in enumerate(distance_data[:, current_node]):
        if dist < 5:  # Nodes within 5 units of the depot get a boost
            scores[idx] -= 0.5
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1160.9910409235176
Sample time  : 2.310514450073242
Evaluate time: 1.096513032913208
Sample orders: 7
======================================================


INFO:absl:Best score of island 0 increased to -1157.184331777223
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            # Favor closer high-demand nodes
            scores[idx] -= adjustment
            if scores[idx] < 0:
                scores[idx] = 0  # Ensure not negative
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            if scores[idx] < 0:
                scores[idx] = 0  # Ensure not negative
    
    for i in range(len(scores)):
        if i == current_node:
            scores[i] = 0  # Skip considering current node
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1157.184331777223
Sample time  : 2.310514450073242
Evaluate time: 0.9087474346160889
Sample orders: 8
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Additional complexity - prioritize nodes with both demand and proximity considerations
    for idx, score in enumerate(scores):
        if node_demands[idx] > 0 and score < 0:
            scores[idx] *= 1.5  # Boost score for nodes with demand and proximity advantage
    
    return -scores
------------------------------------------------------
Score        : -1161.9876066089935
Sample time  : 2.310514450073242
Evaluate time: 0.8915040493011475
Sample orders: 9
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()

    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            # Favor closer high-demand nodes
            scores[idx] -= adjustment
            if scores[idx] < 0:
                scores[idx] = 0
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            if scores[idx] < 0:
                scores[idx] = 0

        # Encourage visiting nearby nodes after visiting a high-demand node
        for j in range(len(scores)):
            if j != idx:
                scores[j] -= math.sqrt(distance_data[idx][j])
                if scores[j] < 0:
                    scores[j] = 0

    for i in range(len(scores)):
        if i == current_node:
            scores[i] = 0

    return -scores
------------------------------------------------------
Score        : -2868.0571088915976
Sample time  : 3.3207093477249146
Evaluate time: 1.4171912670135498
Sample orders: 10
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()

    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
            if scores[idx] < 0:
                scores[idx] = 0
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            if scores[idx] < 0:
                scores[idx] = 0

    for i in range(len(scores)):
        if i == current_node:
            scores[i] = 0
    
    sorted_nodes = np.argsort(-scores)  # Sort nodes based on scores in descending order
    sorted_demand = [node_demands[i] for i in sorted_nodes]
    remaining_capacity_copy = remaining_capacity

    for node_index in sorted_nodes:
        if remaining_capacity_copy >= node_demands[node_index]:
            remaining_capacity_copy -= node_demands[node_index]
        else:
            scores[node_index] = 0

    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -2806.7200678660224
Sample time  : 3.3207093477249146
Evaluate time: 3.8020293712615967
Sample orders: 11
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
            if scores[idx] < 0:
                scores[idx] = 0
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            if scores[idx] < 0:
                scores[idx] = 0
    
    for i in range(len(scores)):
        if i == current_node:
            scores[i] = 0
    
    # Introduce additional complexity for node selection
    for i in range(len(scores)):
        if i % 2 != 0:
            scores[i] *= 1.5
        else:
            scores[i] *= 0.75
        
        if node_demands[i] >= 5:
            scores[i] -= 2.0
        elif node_demands[i] <= 2:
            scores[i] += 1.0

    return -scores
------------------------------------------------------
Score        : -1176.9020147825963
Sample time  : 3.3207093477249146
Evaluate time: 0.9685962200164795
Sample orders: 12
======================================================


INFO:absl:Best score of island 0 increased to -1154.5997547080285
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > 0.5 * remaining_capacity:
                scores[idx] -= 2 * adjustment
            else:
                scores[idx] -= adjustment
            if scores[idx] < 0:
                scores[idx] = 0
        else:
            if demand > 1.25 * remaining_capacity:
                scores[idx] += 2 * adjustment if scores[idx] > 0 else -2 * adjustment
            else:
                scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            if scores[idx] < 0:
                scores[idx] = 0
    
    for i in range(len(scores)):
        if i == current_node:
            scores[i] = 0
    
    return -scores
------------------------------------------------------
Score        : -1154.5997547080285
Sample time  : 3.3207093477249146
Evaluate time: 0.9402029514312744
Sample orders: 13
======================================================


INFO:absl:Best score of island 5 increased to -1136.5166329720525
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
        
    return -scores
------------------------------------------------------
Score        : -1136.5166329720525
Sample time  : 2.3824654817581177
Evaluate time: 1.8865571022033691
Sample orders: 14
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > 0.5:  # Adjust for high-demand nodes
                scores[idx] -= math.log(demand)  # More penalty for very high demand nodes
            else:
                scores[idx] -= math.sqrt(demand)  # Regular penalty for medium demand nodes
        else:
            if demand < 0.3:  # Handle low-demand nodes differently
                scores[idx] -= math.sqrt(demand) * 2  # More penalty for low-demand nodes
            else:
                scores[idx] += adjustment  # Regular penalty for exceeding capacity
    
    return -scores
------------------------------------------------------
Score        : -1157.1402536453281
Sample time  : 2.3824654817581177
Evaluate time: 0.8709390163421631
Sample orders: 15
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    median_demand = np.median(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if demand > median_demand:
            scores[idx] -= 0.5  # Encourage selections with demand above median
        else:
            scores[idx] += 0.5  # Discourage selections with demand below median
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1163.6444924471218
Sample time  : 2.3824654817581177
Evaluate time: 2.2455673217773438
Sample orders: 16
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Custom improvement logic based on node_scores
    for idx, score in enumerate(node_scores):
        if score > 0:
            scores[idx] -= 2 * math.log(score)  # Boost priority for nodes with high scores
        else:
            scores[idx] += 2 * math.log(abs(score))  # Penalize nodes with low scores
    
    return -scores
------------------------------------------------------
Score        : None
Sample time  : 2.3824654817581177
Evaluate time: 0.47425007820129395
Sample orders: 17
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    possible_nodes = [idx for idx, demand in enumerate(node_demands) if demand <= remaining_capacity]
    if len(possible_nodes) > 0:
        closest_node = possible_nodes[np.argmin(distance_data[current_node][possible_nodes])]
        scores[closest_node] -= 1.0  # Prefer closest node that fits into remaining capacity
    
    return -scores
------------------------------------------------------
Score        : None
Sample time  : 4.286592066287994
Evaluate time: 1.3671045303344727
Sample orders: 18
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    max_score = np.max(scores)
    min_score = np.min(scores)
    score_range = max_score - min_score
    
    for idx, score in enumerate(scores):
        if score > min_score + 0.8 * score_range:
            scores[idx] += 2.0  # Boost the score for nodes with high priority
    
    return -scores
------------------------------------------------------
Score        : -1123.0234274825646
Sample time  : 4.286592066287994
Evaluate time: 3.273923873901367
Sample orders: 19
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    for k in range(len(scores)):
        if node_demands[k] > remaining_capacity:
            scores[k] -= 1.0  # Penalize nodes with demand exceeding vehicle capacity
    
    return -scores
------------------------------------------------------
Score        : -1127.9241502209654
Sample time  : 4.286592066287994
Evaluate time: 1.9178557395935059
Sample orders: 20
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
        
        for i in range(len(scores)):
            if distance_data[current_node][i] != 0:
                scores[i] -= 0.1 * distance_data[current_node][i]  # Penalize nodes based on distance from current node
        
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    return -scores
------------------------------------------------------
Score        : -5224.378606238873
Sample time  : 4.286592066287994
Evaluate time: 1.7064995765686035
Sample orders: 21
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    min_distance = np.min(scores)
    max_distance = np.max(scores)
    
    for idx, distance in enumerate(scores):
        if distance == min_distance:
            scores[idx] -= 2  # Further penalize the node closest in terms of distance
        elif distance == max_distance:
            scores[idx] += 2  # Provide extra incentive for the node farthest in terms of distance
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1161.9876066089935
Sample time  : 2.98346871137619
Evaluate time: 1.657663345336914
Sample orders: 22
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_threshold:
            scores[idx] -= adjustment * 2  # Strongly favor high-demand nodes
        elif demand == demand_threshold:
            scores[idx] -= adjustment  # Slightly favor nodes at demand threshold
        elif demand > 0:
            scores[idx] -= adjustment / 2  # Give slight preference to low-demand nodes
        
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Additional bonus for capacity satisfaction
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 2.98346871137619
Evaluate time: 0.49094057083129883
Sample orders: 23
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment

    for i in range(len(scores)):
        if scores[i] < 0:
            scores[i] += abs(scores[i]) * 2  # Increase penalty for negative scores

    max_demand = np.max(node_demands)
    for i in range(len(scores)):
        if node_demands[i] == max_demand:
            scores[i] -= max_demand * 0.1  # Slightly reduce score for node with max demand

    return -scores
------------------------------------------------------
Score        : -1171.2984033075938
Sample time  : 2.98346871137619
Evaluate time: 1.5204815864562988
Sample orders: 24
======================================================


INFO:absl:Best score of island 4 increased to -1154.2975228308105
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > np.mean(node_demands):  # Check if demand is above average
                scores[idx] -= 2 * adjustment  # Further favor high-demand nodes
            else:
                scores[idx] -= adjustment
        else:
            if scores[idx] > 0:  # Check if node is within capacity but high demand
                scores[idx] += adjustment / 2
            else:
                scores[idx] += adjustment
            
    # Additional complex optimization logic here

    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1154.2975228308105
Sample time  : 2.98346871137619
Evaluate time: 1.0086934566497803
Sample orders: 25
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = np.zeros(len(distance_data))
    
    for idx, demand in enumerate(node_demands):
        demand_distance_ratio = demand / (distance_data[current_node][idx] + 1)  # Adding 1 to avoid division by zero
        capacity_utilization = demand / remaining_capacity
        
        if capacity_utilization > 0.5:
            scores[idx] = demand_distance_ratio * 0.8  # Higher weight for demand
        else:
            scores[idx] = demand_distance_ratio * 0.2  # Higher weight for distance
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -5224.378606238873
Sample time  : 2.4856430888175964
Evaluate time: 1.0688896179199219
Sample orders: 26
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    max_demand = np.max(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            
        if demand == max_demand:
            scores[idx] -= adjustment  # Further prioritize nodes with highest demand
        
        if current_node != idx and distance_data[current_node, idx] == 0:
            scores[idx] += 10  # Avoid selecting the same node again if already visited
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1172.2983627885915
Sample time  : 2.4856430888175964
Evaluate time: 1.461606740951538
Sample orders: 27
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Additional complexity
    for i in range(len(scores)):
        if i != current_node:
            scores[i] *= 1.2  # Increase the score of nodes not in the current position
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1161.9876066089935
Sample time  : 2.4856430888175964
Evaluate time: 0.8938663005828857
Sample orders: 28
======================================================


INFO:absl:Best score of island 2 increased to -1157.4630149209227
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    max_demand = np.max(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            if demand == max_demand:
                scores[idx] -= 1  # Prioritize node with highest demand if capacity allows
            else:
                scores[idx] -= 0.5  # Slightly favor nodes with lower demand if within capacity
        else:
            if scores[idx] > 0:
                scores[idx] += 0.5  # Penalize nodes just above capacity
            else:
                scores[idx] += 1  # Penalize nodes exceeding capacity
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1157.4630149209227
Sample time  : 2.4856430888175964
Evaluate time: 1.4358904361724854
Sample orders: 29
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            if current_node == depot_node:
                scores[idx] -= adjustment / 2
            else:
                scores[idx] += adjustment / 2
    
    # Apply additional penalty for skipping high-demand nodes
    for idx, demand in enumerate(node_demands):
        if (scores[idx] < 0) and (demand > np.mean(node_demands)):
            scores[idx] -= math.sqrt(demand) / 2
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 3.055505871772766
Evaluate time: 0.4856112003326416
Sample orders: 30
======================================================


INFO:absl:Best score of island 3 increased to -1160.4767546608982
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Additional complexity - consider node importance based on distance from depot
    depot_distance = distance_data[0]
    for idx in range(len(scores)):
        scores[idx] += 0.1 * depot_distance[current_node] + 0.2 * depot_distance[idx]
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1160.4767546608982
Sample time  : 3.055505871772766
Evaluate time: 0.8905699253082275
Sample orders: 31
======================================================


INFO:absl:Best score of island 3 increased to -1156.6427664183254
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = np.zeros(len(distance_data))
    
    for idx in range(len(distance_data)):
        distance_adjustment = distance_data[current_node][idx]  # Base on distance
        demand_adjustment = math.sqrt(node_demands[idx])  # Base on demand
        
        if remaining_capacity >= node_demands[idx]:
            scores[idx] = -distance_adjustment + demand_adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] = -distance_adjustment - demand_adjustment  # Penalize nodes exceeding capacity
            
    # Apply additional rules based on specific conditions
    for idx, demand in enumerate(node_demands):
        if demand > 0.8 * remaining_capacity:
            scores[idx] *= 1.5  # Increase priority for high-demand nodes close to capacity
        if demand == 0:
            scores[idx] *= 0.5  # Reduce priority for nodes with no demand
        
    return scores
------------------------------------------------------
Score        : -1156.6427664183254
Sample time  : 3.055505871772766
Evaluate time: 1.313192367553711
Sample orders: 32
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
        
        # Introduce additional penalty for nodes far from current position
        distance_penalty = 1 / (1 + math.exp(-(distance_data[current_node][idx] - 5)))  # Sigmoid distance penalty
        scores[idx] += distance_penalty
        
        # Consider connecting nodes with similar demands
        for neighbor_idx, neighbor_demand in enumerate(node_demands):
            if idx != neighbor_idx and abs(demand - neighbor_demand) < 5:
                scores[idx] -= 0.5 * adjustment
        
    return -scores
------------------------------------------------------
Score        : -1269.7638596164181
Sample time  : 3.055505871772766
Evaluate time: 1.3691442012786865
Sample orders: 33
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment * 1.2  # Penalize nodes with demand significantly above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 1.5  # Favor closer high-demand nodes with less penalty
        else:
            scores[idx] += adjustment + distance_data[current_node, idx]  # Penalize nodes exceeding capacity based on distance
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) * 0.6  # Encourage visiting non-demand nodes with less penalty
        
    return -scores
------------------------------------------------------
Score        : -1138.6380917587394
Sample time  : 3.086148202419281
Evaluate time: 2.549034357070923
Sample orders: 34
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
    
    # Custom improvement strategy
    for i in range(len(scores)):
        if i != current_node and node_demands[i] > 0:
            if remaining_capacity >= node_demands[i]:
                scores[i] += np.max(scores) / 4  # Encourage visiting high-demand nodes within capacity
            else:
                scores[i] += np.max(scores) / 2  # Penalize visiting high-demand nodes exceeding capacity
    
    return -scores
------------------------------------------------------
Score        : -1547.6818987027427
Sample time  : 3.086148202419281
Evaluate time: 1.790766716003418
Sample orders: 35
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
        if idx != current_node and idx in np.argsort(scores)[:int(len(scores)*0.2)]:  # Favor top 20% closest nodes
            scores[idx] -= adjustment / 2
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
        if node_demands[i] == 0:
            dist_to_high_demand = np.mean(distance_data[i][np.nonzero(node_demands)[0]])
            if distance_data[current_node][i] <= dist_to_high_demand:
                scores[i] -= np.min(scores) / 2  # Favor nodes closer to high-demand nodes 
        
    return -scores
------------------------------------------------------
Score        : -1152.1109933483544
Sample time  : 3.086148202419281
Evaluate time: 4.539576768875122
Sample orders: 36
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
    
    # Additional complex logic for further optimization
    if np.sum(node_demands) > remaining_capacity * 2:
        for i in range(len(scores)):
            if node_demands[i] < np.median(node_demands):
                scores[i] *= 1.5  # Increase score for nodes below median demand
    
    return -scores
------------------------------------------------------
Score        : -1151.5849180819284
Sample time  : 3.086148202419281
Evaluate time: 3.115671396255493
Sample orders: 37
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    """
    Generates node selection scores balancing distance, demand, and demand priority weight.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        demand_priority_weight: Weight for demand priority (default = 0.5)
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment * demand_priority_weight  # Factor in demand priority weight
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 2.6100107431411743
Evaluate time: 0.46746397018432617
Sample orders: 38
======================================================


INFO:absl:Best score of island 1 increased to -1160.33783468178
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        score_adjustment = math.sqrt(demand)  # Base adjustment value
        
        if remaining_capacity >= demand:
            scores[idx] -= score_adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += score_adjustment if scores[idx] > 0 else -score_adjustment  # Penalize nodes exceeding capacity
        
        # Further adjustments based on distance
        for i in range(len(distance_data)):
            if i != current_node and distance_data[current_node][i] < distance_data[current_node][idx]:
                scores[idx] -= 0.2  # Slightly penalize nodes farther from other nearby nodes
                
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1160.33783468178
Sample time  : 2.6100107431411743
Evaluate time: 0.9195568561553955
Sample orders: 39
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment

    penalty_threshold = 5
    for idx, distance in enumerate(distance_data[current_node]):
        if distance > penalty_threshold:
            scores[idx] += distance ** 2
    
    return -scores
------------------------------------------------------
Score        : -1163.9845149041353
Sample time  : 2.6100107431411743
Evaluate time: 0.9883592128753662
Sample orders: 40
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(node_demands)):
        for j in range(len(node_demands)):
            if i != j and distance_data[i][j] < 10:
                scores[i] *= 1.1  # Increase score for nearby nodes
    
    if remaining_capacity > np.median(node_demands):
        scores = np.power(scores, 2)  # Boost scores for larger capacity vehicles
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1177.981908083006
Sample time  : 2.6100107431411743
Evaluate time: 3.2517249584198
Sample orders: 41
======================================================


INFO:absl:Best score of island 7 increased to -1149.5554022661224
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    min_demand = np.min(node_demands)
    max_demand = np.max(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if demand == min_demand:
            scores[idx] += 1  # Boost score for nodes with minimum demand
        if demand == max_demand:
            scores[idx] -= 1  # Penalize score for nodes with maximum demand
    
    for i in range(len(scores)):
        if i != current_node and i != np.argmin(distance_data[current_node]):
            scores[i] += 0.5  # Slightly boost score for non-closest nodes
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1149.5554022661224
Sample time  : 3.157555937767029
Evaluate time: 2.6815600395202637
Sample orders: 42
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand >= demand_threshold:
            if remaining_capacity >= demand:
                scores[idx] -= adjustment * 2
                if scores[idx] < 0:
                    scores[idx] = 0
            else:
                scores[idx] += adjustment * 2
        else:
            if remaining_capacity >= demand:
                scores[idx] -= adjustment
            else:
                scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 3.157555937767029
Evaluate time: 0.4907100200653076
Sample orders: 43
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            if scores[idx] > 0:
                scores[idx] += adjustment - (demand / remaining_capacity)  # Adjust based on demand and capacity
            else:
                scores[idx] -= adjustment + (remaining_capacity / demand)  # Adjust based on demand and capacity
    
    # Apply additional heuristic based on distance
    for i in range(len(distance_data)):
        if distance_data[current_node][i] < 100:
            scores[i] += 0.5  # Give bonus to nodes closeby
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 3.157555937767029
Evaluate time: 0.8664934635162354
Sample orders: 44
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        demand_penalty = demand * demand_penalty_factor
        
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += demand_penalty
        
        for i in range(len(node_demands)):
            if i != idx and remaining_capacity < node_demands[i]:
                scores[i] += demand_penalty / 2
            
    return -scores
------------------------------------------------------
Score        : None
Sample time  : 3.157555937767029
Evaluate time: 0.49312591552734375
Sample orders: 45
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    max_demand = np.max(node_demands)

    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            if demand == max_demand:
                scores[idx] -= 1  # Prioritize node with highest demand if capacity allows
            else:
                scores[idx] -= 0.5  # Slightly favor nodes with lower demand if within capacity
        else:
            if distance_data[current_node][idx] < 100:
                scores[idx] += 0.5  # Penalize nodes with close distance but exceeding capacity
            elif node_demands[idx] == max_demand:
                scores[idx] += 0.75  # Penalize nodes exceeding capacity but with highest demand
            else:
                scores[idx] += 1  # Penalize nodes exceeding capacity
        
        for jdx, demand_j in enumerate(node_demands):
            if jdx != idx and remaining_capacity >= demand + demand_j:
                combined_distance = distance_data[current_node][idx] + distance_data[idx][jdx]
                if combined_distance < distance_data[current_node][jdx]:
                    scores[jdx] -= 0.25  # Favor neighboring nodes that can be visited sequentially
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1161.2452705162061
Sample time  : 3.323160409927368
Evaluate time: 1.5406041145324707
Sample orders: 46
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    max_demand = np.max(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            if demand == max_demand:
                scores[idx] -= 1  # Prioritize node with highest demand if capacity allows
            else:
                scores[idx] -= 0.5  # Slightly favor nodes with lower demand if within capacity
        else:
            if scores[idx] > 0:
                scores[idx] += 0.5  # Penalize nodes just above capacity
            else:
                scores[idx] += 1  # Penalize nodes exceeding capacity

    for i in range(len(scores)):
        if scores[i] < 0:
            scores[i] *= 1.2  # Increase the penalty for nodes with negative scores
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1157.4630149209227
Sample time  : 3.323160409927368
Evaluate time: 1.4848544597625732
Sample orders: 47
======================================================


INFO:absl:Best score of island 2 increased to -1138.3931711368928
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            scores[idx] -= 0.5  # Slightly favor nodes within capacity
        else:
            scores[idx] += 1  # Penalize nodes exceeding capacity

    sorted_indices = np.argsort(scores)
    sorted_demand = node_demands[sorted_indices]
    
    for i in range(len(sorted_demand)):
        if i % 2 == 0:
            scores[sorted_indices[i]] -= 0.5  # Give a boost to every other node based on demand
        else:
            scores[sorted_indices[i]] += 0.5  # Penalize the remaining nodes
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1138.3931711368928
Sample time  : 3.323160409927368
Evaluate time: 2.819242238998413
Sample orders: 48
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    max_demand = np.max(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            if demand == max_demand:
                scores[idx] -= 1
            else:
                scores[idx] -= 0.5
        else:
            if scores[idx] > 0:
                scores[idx] += 0.5
            else:
                scores[idx] += 1
    
    max_score = np.max(scores)
    min_score = np.min(scores)
    
    for idx, score in enumerate(scores):
        if score == max_score:
            scores[idx] -= 0.5  # Slightly penalize node with highest score
        elif score == min_score:
            scores[idx] += 0.5  # Slightly favor node with lowest score
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1166.7899985715196
Sample time  : 3.323160409927368
Evaluate time: 2.4237656593322754
Sample orders: 49
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            scores[idx] -= 0.5  # Slightly favor nodes within capacity
        else:
            scores[idx] += 1  # Penalize nodes exceeding capacity

    sorted_indices = np.argsort(scores)
    sorted_demand = node_demands[sorted_indices]
    
    for i in range(len(sorted_demand)):
        if i % 2 == 0:
            scores[sorted_indices[i]] -= 0.5  # Give a boost to every other node based on demand
    for i in range(len(sorted_demand)):
        if i % 3 == 0:
            scores[sorted_indices[i]] += 0.2  # Add a bonus to nodes based on demand every third time
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1140.7902634799527
Sample time  : 3.8719922304153442
Evaluate time: 2.848879814147949
Sample orders: 50
======================================================


INFO:absl:Best score of island 2 increased to -1137.2522106918918
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            scores[idx] -= 0.5  
        else:
            scores[idx] += 1  

    sorted_indices = np.argsort(scores)
    sorted_demand = node_demands[sorted_indices]
    
    for i in range(len(sorted_demand)):
        if i % 3 == 0:
            scores[sorted_indices[i]] -= 0.5  
        elif i % 3 == 1:
            scores[sorted_indices[i]] += 0.2  
        else:
            scores[sorted_indices[i]] -= 0.3  
    
    return -scores 
------------------------------------------------------
Score        : -1137.2522106918918
Sample time  : 3.8719922304153442
Evaluate time: 2.8800313472747803
Sample orders: 51
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            scores[idx] -= 0.5  # Slightly favor nodes within capacity
        else:
            scores[idx] += 1  # Penalize nodes exceeding capacity
    
    sorted_indices = np.argsort(scores)
    sorted_demand = node_demands[sorted_indices]
    
    for i in range(len(sorted_demand)):
        if i % 3 == 0:
            scores[sorted_indices[i]] -= 1.0  # Give a boost to every third node based on demand
        elif i % 3 == 1:
            scores[sorted_indices[i]] += 0.3  # Slightly penalize every third node
        else:
            scores[sorted_indices[i]] -= 0.2  # Give a small boost to the rest
        
        if total_demand > 0:
            scores[sorted_indices[i]] += 0.1 * (node_demands[sorted_indices[i]] / total_demand)  # Adjust scores based on demand distribution
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1137.2522106918918
Sample time  : 3.8719922304153442
Evaluate time: 2.8438563346862793
Sample orders: 52
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        if demand >= demand_threshold:
            scores[idx] -= 1  # Penalize nodes with high demand
        else:
            scores[idx] -= 0.5  # Slightly favor nodes with lower demand
        
        if remaining_capacity >= demand:
            scores[idx] -= 0.5  # Slightly favor nodes within capacity
        else:
            scores[idx] += 1  # Penalize nodes exceeding capacity

    sorted_indices = np.argsort(scores)
    sorted_demand = node_demands[sorted_indices]
    
    for i in range(len(sorted_demand)):
        if i % 3 == 0:
            scores[sorted_indices[i]] -= 0.5  # Give a boost to every third node based on demand
        elif i % 3 == 1:
            scores[sorted_indices[i]] += 0.5  # Penalize every other node
        else:
            scores[sorted_indices[i]] -= 0.25  # Slightly boost the remaining nodes
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 3.8719922304153442
Evaluate time: 0.49367809295654297
Sample orders: 53
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment  # Penalize nodes exceeding capacity
    
    sorted_indices = np.argsort(-scores)  # Sort indices in descending order of scores
    
    improved_scores = np.zeros(scores.shape)
    for i, idx in enumerate(sorted_indices):
        improved_scores[idx] = scores[idx] / (i + 1) + 0.1  # Add gradual increase to scores based on position
    
    return improved_scores
------------------------------------------------------
Score        : -5224.378606238873
Sample time  : 3.1636391282081604
Evaluate time: 4.222302198410034
Sample orders: 54
======================================================


INFO:absl:Best score of island 9 increased to -1153.8757198305093
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Add a new criteria to prioritize nodes with higher demands first
    for idx, demand in enumerate(node_demands):
        scores[idx] -= 0.1 * demand  # Penalize low-demand nodes
    
    # Adjust scores based on the distance to the depot
    depot_distance = distance_data[current_node][0]
    for idx, distance in enumerate(distance_data[current_node]):
        scores[idx] -= 0.2 * abs(depot_distance - distance)  # Favor nodes closer to the depot
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1153.8757198305093
Sample time  : 3.1636391282081604
Evaluate time: 1.1552839279174805
Sample orders: 55
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = np.zeros_like(distance_data[current_node])
    
    for idx, demand in enumerate(node_demands):
        if remaining_capacity >= demand:
            distance_penalty = min(distance_data[current_node][idx], np.max(distance_data[current_node]) * max_distance_penalty)
            scores[idx] = demand * demand_priority - distance_penalty
        else:
            capacity_excess = demand - remaining_capacity
            capacity_penalty_value = min(capacity_excess, remaining_capacity) * capacity_penalty
            scores[idx] = -capacity_penalty_value
    
    return scores
------------------------------------------------------
Score        : None
Sample time  : 3.1636391282081604
Evaluate time: 0.4644315242767334
Sample orders: 56
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Additional complexity - adjust scores based on nearby node demands
    for i in range(len(node_demands)):
        if i != current_node:
            nearby_demand = sum(node_demands[i-1:i+2])  # Sum demand of current node and adjacent nodes
            scores[i] -= math.sqrt(nearby_demand)
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1157.2430727977337
Sample time  : 3.1636391282081604
Evaluate time: 0.9730346202850342
Sample orders: 57
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        score_adjustment = math.sqrt(demand)  # Base adjustment value
        
        if remaining_capacity >= demand:
            scores[idx] -= score_adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += score_adjustment if scores[idx] > 0 else -score_adjustment  # Penalize nodes exceeding capacity
        
        # Further adjustments based on distance and demand
        for i in range(len(distance_data)):
            if i != current_node:
                demand_ratio = 1 - (node_demands[idx] / max(node_demands))  # Calculate demand ratio
                distance_ratio = distance_data[current_node][i] / max(distance_data[current_node])  # Calculate distance ratio

                # Apply complex adjustment based on demand and distance ratios
                if demand_ratio > 0.5 and distance_ratio < 0.7:
                    scores[idx] -= 0.5
                elif demand_ratio > 0.3 and distance_ratio < 0.5:
                    scores[idx] -= 0.3
                elif demand_ratio > 0.1 and distance_ratio < 0.3:
                    scores[idx] -= 0.1
                else:
                    scores[idx] += 0.2
                
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1192.33429877134
Sample time  : 4.905848324298859
Evaluate time: 1.911895751953125
Sample orders: 58
======================================================


INFO:absl:Best score of island 1 increased to -1158.7859379847664
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        score_adjustment = math.sqrt(demand)
        
        if remaining_capacity >= demand:
            scores[idx] -= score_adjustment
        else:
            scores[idx] += score_adjustment if scores[idx] > 0 else -score_adjustment
        
        for i in range(len(distance_data)):
            if i != current_node and distance_data[current_node][i] < distance_data[current_node][idx]:
                scores[idx] -= 0.2
                
        if demand > 0.8 * remaining_capacity:
            scores[idx] += 0.5  # Increase priority for high-demand nodes
            
        if current_node % 2 == 0:
            scores[idx] -= 0.3  # Penalize nodes when current node is even
        
        if remaining_capacity <= sum(node_demands) / len(node_demands):
            scores[idx] -= 0.4  # Decrease priority if remaining capacity is low
            
        if np.median(node_demands) == demand:
            scores[idx] -= 0.2  # Slightly penalize nodes with demand equal to median
        
    return -scores
------------------------------------------------------
Score        : -1158.7859379847664
Sample time  : 4.905848324298859
Evaluate time: 3.7521564960479736
Sample orders: 59
======================================================


INFO:absl:Best score of island 1 increased to -1156.6409010648915
================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        score_adjustment = math.sqrt(demand)  # Base adjustment value
        
        if remaining_capacity >= demand:
            scores[idx] -= score_adjustment * 0.8  # Adjusting weight for demand and distance
        else:
            scores[idx] += score_adjustment * 0.5 if scores[idx] > 0 else -score_adjustment  # Adjusting weight for exceeding capacity
        
        # Further adjustments based on distance
        for i in range(len(distance_data)):
            if i != current_node:
                distance_factor = 1 / (1 + math.exp(-distance_data[current_node][i]))  # Sigmoid function
                scores[idx] -= distance_factor * 0.3  # Adjusting weight based on distance
                
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1156.6409010648915
Sample time  : 4.905848324298859
Evaluate time: 0.9647109508514404
Sample orders: 60
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        
        score_adjustment = math.sqrt(demand)  # Base adjustment value
        
        if remaining_capacity >= demand:
            scores[idx] -= score_adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += score_adjustment if scores[idx] > 0 else -score_adjustment  # Penalize nodes exceeding capacity
        
        # Further adjustments based on distance
        for i in range(len(distance_data)):
            if i != current_node and distance_data[current_node][i] < distance_data[current_node][idx]:
                scores[idx] -= 0.2  # Slightly penalize nodes farther from other nearby nodes
                
        # Additional adjustments based on demand spread
        demand_range = np.max(node_demands) - np.min(node_demands)
        demand_ratio = (demand - np.min(node_demands)) / demand_range
        scores[idx] -= 0.5 * demand_ratio  # Penalize nodes with demand far from average
        
        if demand > np.mean(node_demands):
            scores[idx] -= 0.3  # Further penalize nodes with high demand
            
        if remaining_capacity > 0.6 * np.sum(node_demands):
            scores[idx] -= 0.4  # Penalize nodes if remaining capacity is high relative to total demand
            
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1161.2288674428423
Sample time  : 4.905848324298859
Evaluate time: 2.037289619445801
Sample orders: 61
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        
        if remaining_capacity >= demand:
            scores[idx] -= adjustment * remaining_capacity / demand  # Adjust based on demand and remaining capacity
        else:
            scores[idx] += adjustment * (demand - remaining_capacity)  # Penalize nodes exceeding capacity
    
    for i in range(len(scores)):
        if i != current_node:
            scores[i] += distance_data[current_node][i] / 2  # Add a penalty for visiting non-current node
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : None
Sample time  : 3.5557990670204163
Evaluate time: 0.8435802459716797
Sample orders: 62
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    # Introduce clustering based on demand levels
    demand_levels = np.quantile(node_demands, [0.25, 0.5, 0.75])  # Define demand quartiles
    for idx, demand in enumerate(node_demands):
        if demand <= demand_levels[0]:
            scores[idx] *= 0.8  # Decrease score for low demand nodes
        elif demand <= demand_levels[1]:
            scores[idx] *= 1.2  # Increase score for medium demand nodes
        else:
            scores[idx] *= 1.5  # Further increase score for high demand nodes

    # Consider immediate neighbors for next move
    neighbors = np.argsort(distance_data[current_node])[:3]  # Get 3 closest neighbors
    for neighbor in neighbors:
        scores[neighbor] *= 0.9  # Slightly decrease score for neighbors to explore other options
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1165.7811524335652
Sample time  : 3.5557990670204163
Evaluate time: 10.947464227676392
Sample orders: 63
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    max_demand = max(node_demands)
    for idx, demand in enumerate(node_demands):
        if demand == max_demand:
            scores[idx] -= max_demand  # Prioritize the node with the highest demand
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1174.2133123732497
Sample time  : 3.5557990670204163
Evaluate time: 1.0499114990234375
Sample orders: 64
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > 0 and distance_data[current_node][idx] < np.mean(distance_data[current_node]):
                scores[idx] -= 2*adjustment  # Favor closer high-demand nodes with respect to the mean distance
            else:
                scores[idx] -= adjustment  # Favor closer high-demand nodes
        else:
            # Penalize nodes exceeding capacity
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
            
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1164.0602694685354
Sample time  : 3.5557990670204163
Evaluate time: 1.0345306396484375
Sample orders: 65
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > np.mean(node_demands):
                scores[idx] -= 2 * adjustment
            else:
                scores[idx] -= adjustment
        else:
            if scores[idx] > 0:
                scores[idx] += adjustment / 2
            else:
                scores[idx] += adjustment
            
    total_demand = np.sum(node_demands)
    demand_ratio = node_demands / total_demand
    distance_ratio = np.sum(distance_data, axis=1) / np.sum(distance_data)
    
    for idx, _ in enumerate(scores):
        scores[idx] = scores[idx] - (demand_ratio[idx] * 2 + distance_ratio[idx] * 3)
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1154.6390989272163
Sample time  : 3.4259287118911743
Evaluate time: 2.3008906841278076
Sample orders: 66
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > np.mean(node_demands):  
                scores[idx] -= 2 * adjustment  
            else:
                scores[idx] -= adjustment
        else:
            if scores[idx] > 0:  
                scores[idx] += adjustment / 2
            else:
                scores[idx] += adjustment
                
        if idx % 2 == 0:
            scores[idx] += 2  # Boost every 2nd node score
        
        if idx % 3 == 0:
            scores[idx] -= 1  # Penalize every 3rd node score
        
        if idx == current_node:
            scores[idx] += 5  # Prioritize returning to current node
            
        if idx != current_node and idx % 4 == 0:
            scores[idx] -= 3  # Penalize visiting nodes divisible by 4
        
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1155.238536480831
Sample time  : 3.4259287118911743
Evaluate time: 1.0907390117645264
Sample orders: 67
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > np.mean(node_demands):  
                scores[idx] -= 2 * adjustment
            else:
                scores[idx] -= adjustment
        else:
            if scores[idx] > 0:  
                scores[idx] += adjustment / 2
            else:
                scores[idx] += adjustment
            
    for i in range(len(scores)):
        if scores[i] < 0:  
            scores[i] += abs(scores[i])  # Ensure all scores are positive
        
    max_demand_node = np.argmax(node_demands)
    scores[max_demand_node] -= 2 * math.sqrt(node_demands[max_demand_node])  # Penalize the node with maximum demand
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1177.2792779339463
Sample time  : 3.4259287118911743
Evaluate time: 1.2226295471191406
Sample orders: 68
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if remaining_capacity >= demand:
            if demand > np.mean(node_demands):  
                scores[idx] -= 2 * adjustment  
            else:
                scores[idx] -= adjustment
        else:
            if scores[idx] > 0:  
                scores[idx] += adjustment / 2
            else:
                scores[idx] += adjustment
    
    total_demand = sum(node_demands)
    if total_demand < remaining_capacity:
        for idx, demand in enumerate(node_demands):
            if scores[idx] < np.median(scores):
                scores[idx] += math.sqrt(demand) / 2
    
    return -scores  # Convert to negative for minimization
------------------------------------------------------
Score        : -1154.2975228308105
Sample time  : 3.4259287118911743
Evaluate time: 3.424529790878296
Sample orders: 69
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5
                else:
                    scores[j] -= 0.5
            else:
                scores[i] -= 0.1 * (1 / distance_data[i][j])  # Penalize nodes with higher distance
    
    return -scores
------------------------------------------------------
Score        : -1124.648904003311
Sample time  : 4.180691182613373
Evaluate time: 1.6367981433868408
Sample orders: 70
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    for i in range(len(scores)):
        if distance_data[current_node][i] == 0:
            scores[i] -= 0.5  # Penalize nodes with distance 0 from current node
        
        if remaining_capacity < node_demands[i]:
            scores[i] -= 1.0  # Penalize nodes with demand higher than remaining capacity

    return -scores
------------------------------------------------------
Score        : -1127.9241502209654
Sample time  : 4.180691182613373
Evaluate time: 1.735347032546997
Sample orders: 71
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    for i in range(len(scores)):
        if node_demands[i] > remaining_capacity:
            scores[i] -= 0.7  # Penalize nodes with demand higher than remaining capacity
    
    unique_distances = np.unique(distance_data)
    for distance in unique_distances:
        nodes_with_distance = np.where(distance_data == distance)
        nodes_with_distance = nodes_with_distance[0]  # Extract node indices
        for node_idx in nodes_with_distance:
            if node_demands[node_idx] > remaining_capacity:
                scores[node_idx] -= 0.3  # Penalize nodes with high demand and same distance
    
    return -scores
------------------------------------------------------
Score        : -1133.6156083264489
Sample time  : 4.180691182613373
Evaluate time: 10.850216150283813
Sample orders: 72
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment

    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5
                else:
                    scores[j] -= 0.5
    
    for i in range(len(scores)):
        if node_demands[i] > remaining_capacity:
            scores[i] -= 1.0
    
    return -scores
------------------------------------------------------
Score        : -1127.9241502209654
Sample time  : 4.180691182613373
Evaluate time: 1.6576306819915771
Sample orders: 73
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    max_score = np.max(scores)
    min_score = np.min(scores)
    score_range = max_score - min_score
    
    for idx, score in enumerate(scores):
        if score > min_score + 0.8 * score_range:
            scores[idx] += 2.0  # Boost the score for nodes with high priority
    
    selected_nodes = np.argsort(scores)[:remaining_capacity+1]
    top_node = selected_nodes[0]
    for node in selected_nodes[1:]:
        if distance_data[current_node][node] < distance_data[current_node][top_node]:
            top_node = node
    scores[top_node] += 3.0  # Further boost the score for the top node
    
    return -scores
------------------------------------------------------
Score        : -1210.2105554325622
Sample time  : 4.217725098133087
Evaluate time: 5.845594167709351
Sample orders: 74
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    max_score = np.max(scores)
    min_score = np.min(scores)
    score_range = max_score - min_score
    
    for idx, score in enumerate(scores):
        if score > min_score + 0.8 * score_range:
            scores[idx] += 2.0  # Boost the score for nodes with high priority
    
    for i in range(len(scores)):
        if remaining_capacity < max_demand:
            scores[i] -= 1.0  # Penalize nodes if remaining capacity is less than the maximum demand
    
    return -scores
------------------------------------------------------
Score        : -1123.0234274825646
Sample time  : 4.217725098133087
Evaluate time: 2.334686517715454
Sample orders: 75
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    """Advanced version of `priority_v2`."""
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5
                else:
                    scores[j] -= 0.5
    
    max_score = np.max(scores)
    min_score = np.min(scores)
    score_range = max_score - min_score
    
    for idx, score in enumerate(scores):
        if score > min_score + 0.8 * score_range:
            scores[idx] += 2.0
    
    final_scores = []
    for idx, score in enumerate(scores):
        if score < 0:
            final_scores.append(scores[idx] - 1.0)  # Penalize nodes with negative scores
        elif score > 0 and distance_data[current_node][idx] != 0:
            final_scores.append(scores[idx] + 1.0)  # Increase scores for nodes with non-zero distance from current node
        else:
            final_scores.append(scores[idx])
    
    return -np.array(final_scores)
------------------------------------------------------
Score        : -1123.0234274825646
Sample time  : 4.217725098133087
Evaluate time: 2.3950283527374268
Sample orders: 76
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    
    max_demand = np.max(node_demands)
    total_demand = np.sum(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand == max_demand:
            scores[idx] -= adjustment * 1.5  # Give extra priority to node with highest demand
        elif total_demand > 0 and remaining_capacity >= demand:
            demand_ratio = demand / total_demand
            scores[idx] -= adjustment * demand_ratio  # Adjust based on demand ratio
        else:
            scores[idx] += adjustment if scores[idx] > 0 else -adjustment
    
    for i in range(len(scores)):
        for j in range(i+1, len(scores)):
            if distance_data[i][j] == 0:
                if scores[i] < scores[j]:
                    scores[i] -= 0.5  # Penalize nodes with distance 0 and lower score
                else:
                    scores[j] -= 0.5
    
    max_score = np.max(scores)
    min_score = np.min(scores)
    score_range = max_score - min_score
    
    for idx, score in enumerate(scores):
        if score > min_score + 0.8 * score_range:
            scores[idx] += 2.0  # Boost the score for nodes with high priority
            
    improved_scores = []
    for score in scores:
        if score < min_score + 0.2 * score_range:
            improved_scores.append(score + 1.0)  # Further boost the score for low priority nodes
        else:
            improved_scores.append(score)
    
    return -np.array(improved_scores)
------------------------------------------------------
Score        : -1123.0234274825646
Sample time  : 4.217725098133087
Evaluate time: 2.343766450881958
Sample orders: 77
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
        
    if np.sum(node_demands) == 0:
        for i in range(len(scores)):
            scores[i] += np.max(scores) / 2  # If all demand is served, visit any node
        
    return -scores
------------------------------------------------------
Score        : -1136.5166329720525
Sample time  : 3.8595775961875916
Evaluate time: 2.479384422302246
Sample orders: 78
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
            
        if current_node == 0:
            scores[idx] -= distance_data[current_node][idx] / 2  # Prefer nodes closer to depot
        elif current_node == 1:
            scores[idx] += demand * 0.75  # Emphasize demand for nodes near the first delivery
        
        for i in range(len(node_demands)):
            if i != current_node and node_demands[i] == 0:
                scores[i] += distance_data[current_node][i] / 2  # Encourage visiting nodes with no demand
        
    return -scores
------------------------------------------------------
Score        : -1155.1592764072725
Sample time  : 3.8595775961875916
Evaluate time: 1.6140551567077637
Sample orders: 79
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
    
    # Additional improvement logic
    max_demand_node = np.argmax(node_demands)
    min_demand_node = np.argmin(node_demands)
    if remaining_capacity >= node_demands[max_demand_node]:
        scores[max_demand_node] -= np.max(scores) / 2  # Prioritize node with highest demand if capacity allows
    elif remaining_capacity >= node_demands[min_demand_node]:
        scores[min_demand_node] -= np.max(scores) / 2  # Prioritize node with lowest demand if capacity allows
    
    return -scores
------------------------------------------------------
Score        : -1149.188226258835
Sample time  : 3.8595775961875916
Evaluate time: 2.4582433700561523
Sample orders: 80
======================================================


================= Evaluated Function =================
def priority(current_node: int, distance_data: np.ndarray, remaining_capacity: int, node_demands: np.ndarray) -> np.ndarray:
    """
    Generates node selection scores balancing distance and demand.
    
    Args:
        current_node: Current vehicle position
        distance_data: Modified distance matrix
        remaining_capacity: Available vehicle capacity
        node_demands: Demand values for all nodes
        
    Returns:
        np.ndarray: Priority scores (higher = better)
    """
    scores = distance_data[current_node].copy()
    demand_average = np.mean(node_demands)
    max_demand = np.max(node_demands)
    
    for idx, demand in enumerate(node_demands):
        adjustment = math.sqrt(demand)
        if demand > demand_average: 
            scores[idx] -= adjustment  # Penalize nodes with demand above average
        elif remaining_capacity >= demand:
            scores[idx] -= adjustment / 2  # Favor closer high-demand nodes
        else:
            scores[idx] += adjustment  # Penalize nodes exceeding capacity
        
        if demand == max_demand:
            scores[idx] -= adjustment  # Further penalize nodes with maximum demand
    
    for i in range(len(scores)):
        if i != current_node and i not in np.nonzero(node_demands)[0]:
            scores[i] += np.max(scores) / 2  # Encourage visiting non-demand nodes
        elif i != current_node and distance_data[current_node][i] < np.mean(distance_data[current_node]):
            scores[i] -= np.mean(distance_data[current_node]) - distance_data[current_node][i]  # Penalize distant nodes
    
    return -scores
------------------------------------------------------
Score        : -1149.57660898237
Sample time  : 3.8595775961875916
Evaluate time: 2.2485153675079346
Sample orders: 81
======================================================

