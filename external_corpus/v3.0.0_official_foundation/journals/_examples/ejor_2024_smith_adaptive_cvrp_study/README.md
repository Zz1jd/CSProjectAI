# Sample Journal Package

This directory is a skeleton for one journal paper package.

Use:
- Replace placeholder metadata and body text with real paper-specific content.
- Rename the directory once a real paper is selected.
- Convert `.md.example` files to `.md` only after the content is ready for governed ingestion.